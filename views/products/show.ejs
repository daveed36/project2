<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <%- include ('../partials/head.ejs') %>
  </head>
  <body>
    <nav>
      <%- include ('../partials/nav.ejs') %>
    </nav>
    <div class="container">
      <h1>Products Show Page</h1>
      The <%=product.name%> is <%=product.color%>.
      <% if(product.readyToEat === true) { %>
          It is ready to eat.
      <% } else { %>
          It is <strong>NOT</strong> ready to eat.
      <% } %>
      <br>
      <button type="button" name="button"><a href="/products/<%=product._id%>/edit">Edit</a></button>
      <br>
      <br>
      <form action="/products/<%=product._id%>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE"/>
      </form>
      <br>
      <button type="button" name="button">
        <a href="/products">Back</a>
      </button>
    </div>
  </body>
</html>
